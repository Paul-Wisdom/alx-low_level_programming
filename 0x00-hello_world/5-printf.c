#include <stdio.h>

/**
 * main - prints a line of text
 * Return: 0 whem compilation is succesful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
